# Control API Environment Variables
# IMPORTANT: Change these values for production!

# JWT Secret - MUST be at least 32 characters for production
# Generate with: openssl rand -base64 32
JWT_SECRET=dev_jwt_secret_that_is_at_least_32_characters_long

# Admin credentials - CHANGE THESE!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Database connection (must match db.env)
DATABASE_URL=postgres://postgres:password@db:5432/hickory

# Allowed CORS origins - comma separated
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional: Force admin bootstrap on startup
FORCE_BOOTSTRAP=true
